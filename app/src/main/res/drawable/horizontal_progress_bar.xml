<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <solid android:color="#DDDDDD" /> <!-- Background color of the progress bar -->
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <color android:color="@color/green" /> <!-- Stripe color -->
        </clip>
    </item>
</layer-list>
